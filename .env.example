# OpenAI API Key
OPENAI_API_KEY=your_openai_api_key_here

# PostgreSQL Connection URI
# Format: postgresql://username:password@hostname:port/database_name?sslmode=disable
POSTGRES_URI=postgresql://langgraph_user:langgraph_password@localhost:5432/langgraph_db?sslmode=disable

# If you're using Docker Compose from this repo, these are the default credentials
# Otherwise, replace with your actual PostgreSQL credentials
